apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rook-www
spec:
#  storageClassName: rook-ceph-block
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
